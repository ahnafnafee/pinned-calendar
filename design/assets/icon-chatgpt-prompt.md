# ChatGPT / DALL·E prompt — Pinned Calendar app icon

Paste this into ChatGPT (with image generation) to refine the icon. Generate at 1024×1024, then export the flat square version for Play Store (Play applies the rounded mask itself).

---

**Prompt:**

A modern Android app icon for a productivity app called "Pinned Calendar". Flat, geometric, Material You style — clean vector look, no photorealism, no text, no letters.

Composition: a single centered white rounded-rectangle calendar card on a warm orange background. The card has two small dark binder rings poking up from its top edge, a thin divider line near the top, and three short horizontal "agenda" bars inside — the top bar is solid orange (the highlighted current week), the two below are light grey. Overlapping the top-right corner of the card is a push-pin / thumbtack seen head-on: a dark charcoal circle with a smaller orange dot in its center and a tiny soft white highlight, so the calendar reads as literally "pinned".

Background: a smooth diagonal orange gradient from a lighter amber (#F8BC63) in the top-left to a deeper orange (#E07F2C) in the bottom-right, with a very subtle lighter sheen across the top.

Style notes: bold and simple so it stays legible at 48px, generous padding around the calendar, soft long shadow under the card, balanced and centered, full-bleed square (the launcher will round the corners). Single focal object, no clutter, no extra icons, no UI, no drop of realism. High-contrast, friendly, confident.

Palette: orange #E07F2C / #F8BC63, charcoal #2A2018, off-white #FCFBF8, light grey #D8DADD.

---

**Tips**
- If it adds text or numbers on the calendar, append: "absolutely no text, numbers, or letters anywhere."
- For an adaptive icon, ask for "the calendar+pin centered on a transparent background with 25% safe padding" as a separate foreground layer.
- Ask for variations: "show the pin as a side-view thumbtack with a needle" vs. "head-on push-pin" to compare.
