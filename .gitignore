*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
app/build/
.superpowers/

# Release signing — never commit keystores or their credentials
keystore.properties
*.jks
*.keystore
RELEASE_SIGNING.local.md

# Marketing site (web/) — Node + Astro build artifacts (package-lock.json IS committed)
web/node_modules/
web/dist/
web/.astro/

# Design tool scratch (the editable assets under design/assets + design/uploads are kept)
design/tmp/
design/.thumbnail
design/icons.zip
